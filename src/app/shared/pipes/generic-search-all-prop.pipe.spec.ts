import { GenericSearchAllPropPipe } from './generic-search-all-prop.pipe';

describe('GenericSearchAllPropPipe', () => {
  it('create an instance', () => {
    const pipe = new GenericSearchAllPropPipe();
    expect(pipe).toBeTruthy();
  });
});
