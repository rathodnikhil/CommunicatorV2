import { SeachMemberPipe } from './seach-member.pipe';

describe('SeachMemberPipe', () => {
  it('create an instance', () => {
    const pipe = new SeachMemberPipe();
    expect(pipe).toBeTruthy();
  });
});
